
  # Chatbot System Interface

  This is a code bundle for Chatbot System Interface. The original project is available at https://www.figma.com/design/xkQdghYNmryR9U7WwL9EG1/Chatbot-System-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  